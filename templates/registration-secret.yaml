apiVersion: v1
kind: Secret
metadata:
  name: {{ template "common.names.fullname" . }}-registration
  labels:
    {{- include "common.labels" . | nindent 4 }}
stringData:
  registration.yml: |
{{- with .Values.hookshot.secrets.registration }}
    {{- toYaml . | nindent 4 }}
{{- end }}
